import { HighestBidderPipe } from './highest-bidder.pipe';

describe('HighestBidderPipe', () => {
  it('create an instance', () => {
    const pipe = new HighestBidderPipe();
    expect(pipe).toBeTruthy();
  });
});
